# 开发指南

## 🤝 贡献指南

我们欢迎所有形式的贡献！请查看我们的贡献指南：

### 开发流程

1. **Fork项目** - 点击右上角的Fork按钮
2. **创建分支** - `git checkout -b feature/your-feature`
3. **提交更改** - `git commit -m 'Add some feature'`
4. **推送分支** - `git push origin feature/your-feature`
5. **创建PR** - 在GitHub上创建Pull Request

### 代码规范

- **Go代码** - 遵循Go官方代码规范
- **TypeScript** - 使用ESLint和Prettier
- **提交信息** - 使用约定式提交格式
- **测试覆盖** - 新功能需要包含测试用例

## 开发环境设置

### 后端开发

```bash
# 安装依赖
go mod tidy

# 开发模式运行
go run ./cmd/server/. -mode=dev

# 运行测试
go test ./...
```

### 前端开发

```bash
cd ui

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## 项目结构

```
LingEcho/
├── cmd/                    # 应用入口点
│   ├── server/             # 主服务器
│   └── mcp/                # MCP服务
├── internal/               # 内部包
│   ├── models/             # 数据模型
│   ├── handlers/           # HTTP处理器
│   └── services/           # 业务逻辑
├── pkg/                    # 公共包
│   ├── sip/                # SIP协议实现
│   └── hardware/          # 硬件设备支持
├── ui/                     # 前端React应用
│   ├── src/
│   │   ├── components/     # React组件
│   │   ├── pages/          # 页面组件
│   │   └── api/            # API客户端
│   └── public/             # 静态资源
├── services/               # 独立服务
│   ├── vad-service/        # VAD服务
│   └── voiceprint-api/     # 声纹识别服务
└── docs/                   # 文档
```

## API开发

### 添加新的API端点

1. 在相应的路由文件中定义路由
2. 在 `internal/handlers/` 中创建处理器函数
3. 在 `internal/services/` 中添加业务逻辑
4. 更新API文档

### 测试API

```bash
# 使用curl或Postman测试端点
curl -X GET http://localhost:7072/api/endpoint
```

## 数据库迁移

```bash
# 运行迁移
go run ./cmd/server/. -migrate

# 创建新迁移
# 编辑 internal/database/migrations/ 中的迁移文件
```

## 调试

### 后端调试

- 使用Go内置调试器（delve）
- 查看 `logs/` 目录中的日志
- 启用调试模式：`-mode=dev`

### 前端调试

- 使用React DevTools
- 查看浏览器控制台
- 在开发模式下启用source maps

