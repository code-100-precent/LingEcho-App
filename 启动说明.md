# LingEcho 启动说明

## 🚀 快速启动

### 方式一：一键启动（推荐）
双击运行 `start.bat`，会自动启动前后端服务。

### 方式二：分别启动
- **只启动前端**: 双击 `start-frontend-only.bat`
- **只启动后端**: 双击 `start-backend-only.bat`

### 方式三：手动启动

#### 启动后端
```bash
cd server
go run ./cmd/server/main.go -mode=dev
```

#### 启动前端
```bash
cd web
npm run dev
```

---

## 📌 访问地址

启动成功后，访问以下地址：

- **前端界面**: http://localhost:5173
- **后端API**: http://localhost:7072/api
- **API文档**: http://localhost:7072/api/docs

---

## ⚙️ 配置说明

### 必须配置的环境变量（在 .env 文件中）

1. **SESSION_SECRET** - 会话密钥（已设置默认值）
2. **LLM_API_KEY** - 大语言模型API密钥（必须配置）

### 可选配置

- **数据库**: 默认使用 SQLite，无需额外配置
- **LLM模型**: 默认 gpt-3.5-turbo，可改为其他模型
- **端口**: 默认前端5173，后端7072

---

## 🔧 常见问题

### 1. 端口被占用
如果提示端口被占用，可以修改 .env 文件中的 `ADDR` 配置：
```
ADDR=:8080  # 改为其他端口
```

### 2. LLM API 密钥配置
编辑 `.env` 文件，找到以下配置：
```env
LLM_API_KEY=sk-your-api-key-here  # 替换为你的真实API密钥
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-3.5-turbo
```

支持的 LLM 提供商：
- OpenAI (gpt-3.5-turbo, gpt-4)
- DeepSeek (deepseek-v3.1)
- 其他兼容 OpenAI API 的服务

### 3. 前端无法连接后端
确保后端服务已启动，并检查控制台是否有错误信息。

### 4. Go 依赖下载慢
可以设置 Go 代理：
```bash
go env -w GOPROXY=https://goproxy.cn,direct
```

---

## 📦 可选服务

项目还包含以下可选的 Python 微服务（需要 Python 3.10+）：

### VAD 服务 (语音活动检测)
```bash
cd services/vad-service
python vad_service.py
```
端口: 7073

### 声纹识别服务
```bash
cd services/voiceprint-api
python -m app.main
```
端口: 7074

### ASR-TTS 服务 (语音识别与合成)
```bash
cd services/asr-tts-service
python -m app.main
```
端口: 7075

**注意**: 这些服务是可选的，不启动也不影响主服务运行。

---

## 🛑 停止服务

- 如果使用 `start.bat` 启动，关闭打开的命令行窗口即可
- 或者在命令行窗口中按 `Ctrl + C` 停止

---

## 📚 更多信息

- 项目文档: README.md
- 中文文档: README_CN.md
- 环境配置示例: server/env.example
